getBot
getBot(string botname)

Return bots from list

Example:

bot = getBot("growid")
bot:sendPacket("action|respawn", 2)
sendPacket
sendPacket( string packet, int type)

Sends text packet with selected type to client .

Example:

-- Sends respawn packet to server
sendPacket("action|respawn", 2)
sendPacketRaw
sendPacketRaw( GamePacket packet)

Sends GamePacket to server.

Example:

-- Sends wear clothing packet to server
packet = {}
packet.type = 10 
packet.int_data = 48 -- Clothing ID (Jeans)
sendPacketRaw(packet)
findPath
findPath(int x, int y)

Finds path to selected x,y

Example:

-- Finds path to top left corner of the world
findPath(0, 0)
getLocal
getLocal()

Returns local NetAvatar struct

Example:

-- Logs local players name
me = getLocal()
log(me.name)
getInventory
getInventory()

Returns table of InventoryItems

Example:

-- Logs all item ids in your inventory
for _,cur in pairs(getInventory()) do
log(cur.id)
end
getPlayers
getPlayers()

Returns table of NetAvatars

Example:

-- Logs current worlds players names
for _,player in pairs(getPlayers()) do
log(player.name)
end
getObjects
getObjects()

Returns table of WorldObjects

Example:

-- Logs current worlds objects item id's
for _,object in pairs(getObjects()) do
log(object.id)
end
getTile
getTile(int x, int y)

Returns world Tile in selected position

Example:

-- Logs top left corners foreground block id
tile = getTile(0, 0)
log(tile.header.fg)
getTiles
getTiles()

Returns table of Tiles

Example:

-- Logs current worlds all foreground block id's
for _,tile in pairs(getTiles()) do
log(tile.header.fg)
end
getItemInfo
getItemInfo(int itemid)

Returns ItemInfo of selected Item ID

Example:

-- Logs Item ID 2's name (Dirt)
log(getItemInfo(2).name)
isSolid
isSolid(int x, int y)

Returns true if block is solid and false if its not

Example:

if isSolid(0, 0) then
log("Tile 0, 0 is solid")
else
log("Tile 0, 0 is not solid")
end
isSeed
isSeed(int x, int y)

Returns true if tile is seed , false if its not

Example:

if isSeed(0, 0) then
log("Tile 0, 0 is Seed")
else
log("Tile 0, 0 is not Seed")
end
canHarvest
canHarvest(int x, int y) Returns true if tile is harvestable , false if not

Example:

if canHarvest(0, 0) then
log("Tile 0, 0 is harvestable")
else
log("Tile 0, 0 is not harvestable")
end
hitTile
hitTile(int x, int y)

Send Punch Packet To X,Y

Example:

---Punch To 0,0
hitTile(0,0)
wrenchTile
wrenchTile(int x, int y)

Send Wrench Packet To x,y

Example:

---Wrench To 0,0

wrenchTile(0,0)
placeTile
placeTile(int x,int y,int blockid)

Place Block To x,y

Example:

---Place Dirt To 0,0
placeTile(0,0,2)
Warp
Warp(string x)

Warp To x

Example:

warp("BUYGHC")
Move
Move(string MOVE,int speed)

Example:

-- Speed Default Value 1, Max Value 4
Move("RIGHT",1)
Move("LEFT",1)
Move("UP",1)
Move("DOWN",1)
setPos
setPos(int x,int y)

Example:

--- Sets bot position
setPos(0,0)
Say
Say(string text)

Example:

-- Send chat message
Say("Hello")
RunThread
RunThread(function() //code here end)

Example:

RunThread(function()
while true do
log("New Thread")
end
end)

while true do
log("Normal Thread")
end
GetAllBot
Example:

for k,v in pairs(GetAllBot) do 
v:Say("hi")
end
Connect
Connect(string growid,string password,bool VdsSupport)

Example:

Connect("mygrowid","mypassword",false)
